# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r:
- oldrel
- release
- devel

after_success:
  - Rscript -e 'covr::codecov(function_exclusions = c("opposite", "new_env"), line_exclusions = list("R/assert.R"))'
